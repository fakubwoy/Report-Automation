services:
  db:
    image: postgres:15-alpine
    container_name: mfg_postgres
    environment:
      POSTGRES_DB: manufacturing
      POSTGRES_USER: mfg_user
      POSTGRES_PASSWORD: mfg_pass123
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - mfg_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mfg_user -d manufacturing"]
      interval: 5s
      timeout: 3s
      retries: 5

  plc-simulator:
    build: .
    container_name: mfg_plc_sim
    command: python src/opcua_simulator.py
    networks:
      - mfg_network
    ports:
      - "4840:4840"
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import socket; sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM); result = sock.connect_ex((\"127.0.0.1\", 4840)); sock.close(); exit(0 if result == 0 else 1)'"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  api-server:
    build: .
    container_name: mfg_api_server
    depends_on:
      plc-simulator:
        condition: service_healthy
      db:
        condition: service_healthy
    env_file: .env
    environment:
      - OPCUA_SERVER_URL=opc.tcp://plc-simulator:4840/freeopcua/server/
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=manufacturing
      - DB_USER=mfg_user
      - DB_PASSWORD=mfg_pass123
    ports:
      - "8000:8000"
    volumes:
      # KEY CHANGE: Mount current directory to /app for live code reloading
      - .:/app
      - ./reports:/app/reports
      - ./dashboard:/app/dashboard
    networks:
      - mfg_network
    command: uvicorn src.api_server:app --host 0.0.0.0 --port 8000 --reload

  reporting-app:
    build: .
    container_name: mfg_reporting_pipeline
    depends_on:
      plc-simulator:
        condition: service_healthy
      db:
        condition: service_healthy
      api-server:
        condition: service_started
    env_file: .env
    environment:
      - OPCUA_SERVER_URL=opc.tcp://plc-simulator:4840/freeopcua/server/
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=manufacturing
      - DB_USER=mfg_user
      - DB_PASSWORD=mfg_pass123
    volumes:
      # KEY CHANGE: Mount current directory to /app
      - .:/app
      - ./reports:/app/reports
      - ./data/processed:/app/data/processed
    networks:
      - mfg_network
    command: python src/scheduler.py

  dashboard:
    image: nginx:alpine
    container_name: mfg_dashboard
    depends_on:
      - api-server
    ports:
      - "8080:80"
    volumes:
      - ./dashboard/dashboard.html:/usr/share/nginx/html/index.html:ro
    networks:
      - mfg_network

networks:
  mfg_network:
    driver: bridge

volumes:
  postgres_data: